# Security Baseline (Pilot)

- TLS termination supported
- RBAC (Admin/Reviewer/Approver/Viewer)
- Tenant isolation by org_id
- Signed URLs for downloads (expiry)
- Audit logs (append-only)
- Retention config (delete after X months)
- Backups guidance (DB daily + object store snapshot)

